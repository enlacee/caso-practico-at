version: '3'
services:
  backend-laravel:
    build: ./backend-laravel
    volumes:
      - ./backend-laravel:/usr/app
    ports:
      - 8000:8000
    command: composer install && php artisan serve --host=0.0.0.0 --port=8000